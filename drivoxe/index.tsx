import { AppRegistry } from 'react-native';
import App from './App'; // Import the main App component
AppRegistry.registerComponent("main", () => App); // Register the main App component with the name from app.json
